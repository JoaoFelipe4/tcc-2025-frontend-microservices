version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    # MUDE ESTA LINHA - adicione /browser no final
    baseDirectory: dist/frontend/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*